# 芯鉴知微实现状态

最后更新：2026-07-21（Phase 7）

## 总体状态

- 当前阶段：Phase 7 教师端、聚合 API 和响应式数据看板已完成实现与验收。
- 下一阶段：Phase 8 尚未开始。
- Git：Phase 6 已提交为 `316aea31a05e9b53dc17e4f79320ff66bdda42f8 feat: complete phase 6 student dashboard`；Phase 7 变更尚未提交。
- 运行状态：长期 Docker 容器未停止或替换；Phase 7 浏览器验收使用独立临时 SQLite 后端与现有 Vite 开发服务。

## Phase 7 实现情况

- [x] 临时教师入口沿用默认关闭的 `REVIEW_ACCESS_TOKEN` 边界，明确不等同于正式教师账号。
- [x] `GET /api/v1/teacher/dashboard` 聚合真实数据库中的设备、日志、诊断和故障树介入记录。
- [x] 在线、离线、未上报和异常状态在设备状态图中互斥，图中设备总数不会重复计算异常设备。
- [x] 高频错误排行基于全部已保存诊断，异常设备列表基于每台设备的最新诊断，七日趋势按诊断时间聚合。
- [x] 班级进度、实验完成率、学生身份和知识案例明确返回未配置状态，不填入参考图中的演示数字或姓名。
- [x] 教师端实现设备状态图、错误横向柱状图、错误趋势图、异常表、单设备日志、介入列表和知识审核空入口。
- [x] 支持搜索过滤、异常设备选择、侧栏折叠、手动刷新、30 秒刷新、错误恢复和退出临时会话。
- [x] 左侧菜单收敛为三个真实区块入口，文案和上下顺序与页面一致，并支持点击定位、滚动高亮、`aria-current` 与键盘焦点。
- [x] 参考图的深蓝看板结构、信息层级、状态配色和密集布局已适配桌面、平板和移动端。

## 数据库迁移

- Phase 7 为只读聚合和前端实现，没有新增业务表或 Alembic 迁移。
- 当前迁移头仍为 `20260720_0004`；学生、班级、实验任务和知识案例不能以空表或假数据冒充完成。

## 验证结果

| 验证项 | 命令/方式 | 结果 |
| --- | --- | --- |
| 后端静态检查 | `ruff check app tests` | 通过 |
| 后端测试 | `pytest` | 37 passed |
| 前端静态检查 | Prettier、ESLint、Vue TypeScript | 通过 |
| 前端单元测试 | `vitest run` | 通过 |
| 前端生产构建 | `npm run build` | 0.7.0 构建通过；仅有 ECharts 分块大小提示 |
| 浏览器桌面布局 | 内置浏览器，1680×940 | 四列统计和四列分析布局；无水平溢出 |
| 浏览器移动布局 | 内置浏览器，390×844 | 两列统计、单列分析和底部三项导航；无水平溢出 |
| 浏览器交互 | 上下滚动、三项菜单定位与高亮、搜索、折叠/展开、刷新、退出边界 | 核心交互可用；长页面可上下滑动，菜单与区块同步，搜索可将异常表过滤为 0 条 |
| 视觉对照 | 参考图与最终浏览器截图同屏比较 | 无未解决 P0/P1/P2；数据稀疏差异属于禁止伪造数据的产品约束 |
| 长期容器 | 未执行停止、重建或替换 | 保持原运行状态 |

## 视觉证据

- 源图：`docs/design-qa/phase7-teacher-ui/reference.png`
- 最终桌面：`docs/design-qa/phase7-teacher-ui/dashboard-viewport.png`
- 菜单审查前后：`docs/design-qa/phase7-teacher-ui/menu-audit-before.png`、`docs/design-qa/phase7-teacher-ui/menu-audit-after.png`
- 移动端：`docs/design-qa/phase7-teacher-ui/dashboard-mobile.png`
- 同屏对照：`docs/design-qa/phase7-teacher-ui/full-comparison.jpg`
- QA 报告：项目根目录 `design-qa.md`

## 已知边界与风险

1. 目前只能识别异常设备，不能证明异常属于某位学生；正式学生、教师、班级、实验任务和角色授权尚未建立。
2. 实验完成率和班级进度没有可计算数据，教师端显示“未配置”，不使用参考图中的示例百分比。
3. 知识审核入口没有案例可审；Phase 8 尚无知识表、Embedding 或 pgvector 知识记录。
4. 当前诊断仍只依赖数据库采集记录、YAML 示例规则和占位故障树，没有真实硬件资料、知识库或 AI 模型支持。
5. 临时教师令牌保存在浏览器 `sessionStorage`，正式账号阶段必须替换为服务端会话与角色权限。
6. ECharts 公共渲染分块约 517 kB，构建通过但超过 Vite 默认提示线，后续可继续按图表类型拆分。

## 下一阶段

Phase 8 将建立来源可追溯的 RAG 知识库。当前已按阶段门禁停止，不进入 Phase 8，也不自动提交 Phase 7；等待用户明确指示。
